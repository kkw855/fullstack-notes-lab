postgres-config {
  n-threads = 32
  url = "jdbc:postgresql://crud-postgres-rw.postgres:5432/notes_db"
  user = "admin"
  pass = "5q5SP5RIoaq7ZlmBxmtJ4lPjqoXhvcufCJBOngOEEB3PaZckSQ4RZ3vgeU2A2hXP"
}

ember-config {
  host = "0.0.0.0"
  port = 5002
}

redis-config {
  host = "redis"
  port = 26379
  pass = "Password123!"
  master-set = "mymaster"
  use-system-dns-resolver = false
  // 물음표(?)가 있기 때문에, 환경 변수가 없으면 이 두 번째 줄은 아예 없는 줄처럼 무시됩니다.
  use-system-dns-resolver = ${?REDIS_USE_SYSTEM_DNS_RESOLVER}
}
